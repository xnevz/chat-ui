import { useContext } from 'react'
import { Navigate } from 'react-router-dom';
import { UserDataContext } from '../../contexts/UserDataContext'

export default function PrivateRouteNoContext({ element }: { element: JSX.Element }) {

}
